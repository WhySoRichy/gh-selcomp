# =====================================================
# Portal Gestión Humana - Configuración de Entorno
# =====================================================
# Copie este archivo a .env y configure los valores

# -----------------------------------------------------
# APLICACIÓN
# -----------------------------------------------------
# URL base de la aplicación (con trailing slash)
BASE_URL=/gh/

# Entorno: development | production
ENVIRONMENT=development

# -----------------------------------------------------
# BASE DE DATOS (MySQL)
# -----------------------------------------------------
DB_HOST=localhost
DB_NAME=gestionhumana
DB_USER=root
DB_PASS=

# -----------------------------------------------------
# SEGURIDAD
# -----------------------------------------------------
# Clave secreta para tokens CSRF (generar una única)
CSRF_SECRET=GENERAR_UNA_CLAVE_SECRETA_AQUI_32_CHARS

# Usar cookies seguras (true en producción con HTTPS)
SESSION_SECURE=false

# Clave de cifrado para secretos TOTP 2FA (AES-256-CBC, 64 hex chars)
# Generar con: php -r "echo bin2hex(random_bytes(32));"
APP_KEY=GENERAR_CON_php_-r_echo_bin2hex_random_bytes_32

# -----------------------------------------------------
# EMAIL (SMTP)
# -----------------------------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu-app-password
SMTP_SECURE=tls
SMTP_FROM_NAME=Portal Gestión Humana

# -----------------------------------------------------
# IA - EXTRACTOR DE CVs (Opcional)
# -----------------------------------------------------
# API Key de Groq para Llama 3.1
GROQ_API_KEY=gsk_XXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Ruta completa de Python (dejar vacío para detectar automáticamente)
# Windows: C:\Python313\python.exe  o  C:\Users\usuario\AppData\Local\Programs\Python\Python311\python.exe
PYTHON_PATH=

# Ruta completa de Tesseract OCR (dejar vacío para detectar automáticamente)
# Se usa como fallback para PDFs basados en imagen (Canva, escaneados)
# Windows: C:\Program Files\Tesseract-OCR\tesseract.exe
# Linux: /usr/bin/tesseract
# Instalar desde: https://github.com/UB-Mannheim/tesseract
TESSERACT_PATH=

# =====================================================
# EXTENSIONES PHP REQUERIDAS
# =====================================================
# pdo_mysql   - Conexión a base de datos
# openssl     - Cifrado AES-256-CBC de secretos TOTP
# gd          - Redimensión de avatares
# fileinfo    - Validación MIME de archivos subidos
# mbstring    - Manejo de strings multibyte (UTF-8)
#
# Verificar con: php -m | grep -E "pdo_mysql|openssl|gd|fileinfo|mbstring"
# =====================================================

# =====================================================
# NOTAS DE SEGURIDAD
# =====================================================
# - NUNCA subas el archivo .env a git
# - En producción usa HTTPS y SESSION_SECURE=true
# - Genera CSRF_SECRET con: php -r "echo bin2hex(random_bytes(32));"
# - Genera APP_KEY con:     php -r "echo bin2hex(random_bytes(32));"
# - Para Gmail, usa App Passwords en vez de tu contraseña real
#   https://myaccount.google.com/apppasswords
# =====================================================
